import { Package } from 'lucide-react'
import { ComingSoon } from '@/components/coming-soon'

export default function ListingsPage() {
  return (
    <ComingSoon
      title="Listings"
      phase="Phase 6"
      icon={Package}
      description="Generate, version, and export optimized marketplace listings from your catalog."
    />
  )
}
